# Backend API base URL (no trailing slash).
# Development: use your local backend (e.g. http://localhost:4000).
# Production (Vercel): set in Vercel project settings to your deployed backend URL.


# Clerk (create .env.local with your key; do not commit .env.local)
# VITE_CLERK_PUBLISHABLE_KEY=pk_test_...
